using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovingJewelArrays : MonoBehaviour
{

    public GameObject[] MovingJewelIconsBlue = new GameObject[5];  //Images of One to Four Jewels fly, depending on Bond Energy
    public GameObject[] MovingJewelIconsRed = new GameObject[5];  //Images of One to Four Jewels fly, depending on Bond Energy

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
