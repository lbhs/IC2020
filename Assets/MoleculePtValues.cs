using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoleculePtValues : MonoBehaviour
{
    public int BonusPtValue;


    // Start is called before the first frame update
    void Start()
    {
        //print(BonusPtValue);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
